import Categories from '@/components/Categories';
import React from 'react'

export const metadata = {
  title: "Meal categories • Racip",
  description: "Browse recipe categories.",
};

const page = () => {
  return (
      <Categories/>
  )
}

export default page